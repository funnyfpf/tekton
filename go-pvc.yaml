kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: go-dependence
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
  volumeMode: Filesystem
